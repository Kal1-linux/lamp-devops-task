# Docker Compose Environment Variables Template
# Copy to `.env` for local testing. Do NOT commit `.env` to version control.

DB_HOST=mysql
DB_PORT=3306
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=apppassword
MYSQL_ROOT_PASSWORD=rootpassword
